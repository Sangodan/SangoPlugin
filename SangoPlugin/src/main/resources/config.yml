# This is the main settings config for SangoPlugin.

# True for checkpoint clay to be enabled by default, false for disabled.
# This setting won't be changed in-game by /switchcheckpointclay
checkpoint_clay: true


# Make checkpoint clay world-independent
# This setting won't be changed in-game by /switchclayworldindependent
clay_independent: true